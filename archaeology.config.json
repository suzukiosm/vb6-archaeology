{
  "$schema_comment": "vb6-archaeology kit configuration. Agents: Read AGENTS.md then docs/ai-onboarding.md.",
  "protected_source_dirs": [
    "source"
  ],
  "default_source_dir": "source",
  "extracts_dir": "working/extracts",
  "reports_dir": "working/reports",
  "skeletons_dir": "working/skeletons",
  "encoding": "cp932",
  "encoding_fallbacks": ["utf-8-sig", "utf-8"],
  "reports_http_port": 8765,
  "geometry_hints": {},
  "deep_read_name_map": {},
  "notes": {
    "protected": "Directories listed in protected_source_dirs are read-only. Hooks deny Write/StrReplace/Delete into them. Exception: python tools/make_fixture.py may regenerate source/mini_vbp/. Rename via this list if your immutable tree is not called source/.",
    "geometry_hints": "Optional map of FormName → {left,top,width,height} twips used when evaluating parent-relative runtime expressions.",
    "deep_read_name_map": "Optional map of VB_Name → output key for frm_deep_read_all.py (e.g. {\"MDIForm1\": \"mdi\"}). Empty = always lowercase VB_Name.",
    "consumer": "When adopting this kit, set protected_source_dirs to your immutable VB6 tree name(s). Use requires LICENSE permission."
  }
}
